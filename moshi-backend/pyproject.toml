[project]
name = "moshi-backend"
version = "0.1.0"
description = "Moshi realtime voice backend for Mirage (Lightning AI dev / Modal prod)"
requires-python = ">=3.10"

[tool.pytest.ini_options]
asyncio_mode = "auto"